// Question No-13 //

var QuestionNo = "Question No-13)"

var arrOfBike = ["Honda", "Super power", "United", "union Start"];

for (let i = 0; i < arrOfBike.length; i++) {

    const element = arrOfBike[i];
    document.writeln(QuestionNo, ` I would like to own a ${element} motorcycle. , <br />`);
}
